## 🌸 Primavera

La primavera es la época de cortejo en muchas especies de aves.
Durante esta temporada, los pájaros realizan pequeños bailes, despliegues y movimientos llamativos cuidadosamente coreografiados para atraer y conquistar a su interés romántico.

## Requisitos
- Python 3.9+ (recomendado 3.10+)
- pip

## Instalación (macOS/Linux)
```bash
git clone 
cd bird-courtship

python3 -m venv .venv
source .venv/bin/activate

python -m pip install -U pip
python -m pip install -r requirements.txt